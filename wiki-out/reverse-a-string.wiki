=={{header|ghul}}==

<syntaxhighlight lang="ghul">
use IO.Std.write_line;
use Ghul.Pipes;

reverse(s: string) -> string =>
    s |> reduce("", (acc, c) => "{c}{acc}");

write_line(reverse("asdf"));
write_line(reverse("Hello, World!"));
</syntaxhighlight>

{{out}}
<pre>
fdsa
!dlroW ,olleH
</pre>
