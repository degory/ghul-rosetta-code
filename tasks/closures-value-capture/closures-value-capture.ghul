use IO.Std.write_line;
use Collections.LIST;

let squares = LIST[() -> int]();

for i in 1::10 do
    // each iteration binads a new i, and each closure captures
    // the i from its own iteration, so each closure returns a
    // different square
    squares.add(() => i * i);
od

write_line("{squares[3]()}");
