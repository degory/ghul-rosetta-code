use IO.Std.write_line;
use Collections.LIST;

let squares = LIST[() -> int]();

// The loop variable is a fresh local variable on each iteration, so each function captures the
// value that iteration had rather than sharing one variable that ends up at ten.
for i in 1::10 do
    squares.add(() => i * i);
od

write_line("{squares[3]()}");
