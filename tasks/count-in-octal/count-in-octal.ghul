use IO.Std.write_line
use System.Convert

// Counting to the largest value the numeric type holds, which for `ubyte`
// is 255 - octal 377.

for value in cast int(ubyte.min_value) :: cast int(ubyte.max_value) do
    write_line(Convert.to_string(value, 8))
od
