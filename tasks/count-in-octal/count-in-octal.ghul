use IO.Std.write_line
use System.Convert

// Counting to the largest value the numeric type holds, which for `ubyte`
// is 255 - octal 377.

for value in ubyte.min_value::ubyte.max_value do
    write_line(Convert.to_string(cast int(value), 8))
od
