use default

let encoding = IO.Std.output_encoding.web_name

if encoding !~ "utf-8" then
    throw System.Exception(
        "this terminal encodes output as {encoding}, not UTF-8"
    )
fi

write_line("△")
