use IO.Std.write_line
use Collections.MAP

let ages = MAP[string, int]()

ages.add("alice", 30)
ages["bob"] = 27

write_line("alice is {ages["alice"]}")
write_line("bob is {ages["bob"]}")
write_line("carol is {if ages.contains_key("carol") then "known" else "not known" fi}")
