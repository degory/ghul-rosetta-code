use IO.Std.write_line

is_leap_year(year: int) -> bool =>
    year % 4 == 0 /\ (year % 100 != 0 \/ year % 400 == 0)

for year in [1900, 1994, 1996, 2000, 2024, 2100] do
    write_line("{year} is a {if is_leap_year(year) then "leap" else "common" fi} year")
od
