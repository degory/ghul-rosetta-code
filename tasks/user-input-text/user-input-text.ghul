use default
use IO.Std.write
use IO.Std.read_line

write("string: ")

let text = read_line()

write("integer: ")

let number = read_line()

if text? /\ number? then
    write_line(
        "read the string {text} and the integer {int.parse(number)}"
    )
fi
