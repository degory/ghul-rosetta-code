{
  "title": "System time",
  "slug": "system-time",
  "state": "solved",
  "note": "reopened, was rejected/nondeterministic: the time differs every run, so the test checks its shape and that it is within a minute of the clock"
}
