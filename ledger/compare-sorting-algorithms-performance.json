{
  "title": "Compare sorting algorithms' performance",
  "slug": "compare-sorting-algorithms-performance",
  "state": "solved",
  "note": "reopened, was rejected/nondeterministic: timings depend on the machine, so the test checks the shape of the reports and plots and that insertion sort is quadratic on shuffled input"
}
