{
  "title": "Hostname",
  "slug": "hostname",
  "state": "solved",
  "note": "reopened, was rejected/nondeterministic: the name depends on the machine, so the test compares it with the one the kernel reports"
}
